{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All",
            "type": "shell",
            "command": "${workspaceFolder}/build-all.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}